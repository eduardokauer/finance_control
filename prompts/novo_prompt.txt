Este arquivo não é mais a fonte canônica do início de ciclo PM.

Use `docs/pm_cycle_start_prompt.md` como prompt oficial do projeto.

Se este arquivo ainda for usado por compatibilidade, ele deve servir apenas como ponte para o artefato canônico em `docs/` e não como fonte paralela de verdade do processo.

