Este arquivo não é mais uma fonte oficial de processo ou de início de ciclo.

Use `docs/pm_cycle_start_prompt.md` para iniciar um novo ciclo PM/LLM.

Se este arquivo ainda for consultado por compatibilidade, trate-o apenas como ponte para os artefatos canônicos em `docs/` e não como fonte paralela de verdade.
